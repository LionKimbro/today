# `disk.py`

OWNS: JSON day-bundle files beneath the hard-coded Today data root.

READS: `LOAD_DAY` Mobile Stacks.

WRITES: `WRITE_DAY` Mobile Stacks.

DOES NOT OWN: live application meaning, canonical Mem records, or Tk state.
